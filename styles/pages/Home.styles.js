import styled from "styled-components"
import Image from "next/image"

export const Home = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rem;
`

export const CasualBanner = styled(Image)`

`